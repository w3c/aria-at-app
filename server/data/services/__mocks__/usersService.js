const UsersService = {
    addUser: user => {
        // eslint-disable-next-line no-unused-vars
        return new Promise((resolve, reject) => {
            resolve(user);
        });
    }
};

module.exports = UsersService;
