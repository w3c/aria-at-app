import React from 'react';
import { Helmet } from 'react-helmet';
import { Container } from 'react-bootstrap';

const TestPlanReportPage = () => {
    const title = '';

    return (
        <Container as="main">
            <Helmet>{title}</Helmet>
        </Container>
    );
};

export default TestPlanReportPage;
